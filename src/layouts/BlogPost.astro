---
import BlogHeader from "../components/BlogHeader.astro";
import BlogPage from "../components/BlogPage.astro";

const { content } = Astro.props;
const { title, description, pubDate, heroImage, alt } = content;
---

<BlogPage {title} {description}>
    <BlogHeader title="danisaacs.net" />

    {
        heroImage && (
            <div class=" h-auto z-10">
                <img class="h-full w-full " loading="eager" src={heroImage} alt={alt} />
                
            </div>
        )
    }

    <div class="flex justify-center text-zinc-700">
        <div class="text-lg w-5/6 lg:w-3/4 prose">
            <h1 class="text-2xl font-semibold mt-8">{title}</h1>
            <p class="font-extralight mb-8">{pubDate}</p>

            <div>
                <p class="text-lg my-6">
                    {description}
                </p>
            </div>
            <div class="prose text-lg">
                <slot />
            </div>
        </div>
    </div>
</BlogPage>
